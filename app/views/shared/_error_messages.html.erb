<% if object.errors.any? %>
  <div class="alert alert-danger">
    The form contations <%= pluralize(object.errors.count, "error") %>.
  </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <strong><li class="text-danger"><%= msg %></li></strong>
      <% end %>
    </ul>
<% end %>
